#include<stdio.h>
#include<string.h>
// program name
int main(){
     // start coding
char country[] = "India";
int i = strlen(country);
printf("length of %s is %d bytes.", country, i);
    return 0;
}